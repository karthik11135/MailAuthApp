<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Title</title>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-iYQeCzEYFbKjA/T2uDLTpkwGzCiq6soy8tYaI1GyVh/UjpbCx/TYkiZhlZB6+fzT"
      crossorigin="anonymous"
    />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@48,400,0,0"
    />
  </head>

  <body>
    <header>
      <nav class="navbar m-2 mx-4 px-2 navbar-expand-lg navbar-light ">
        <a class="navbar-brand fs-4 fw-semibold" href="/">MAILER</a>
        <button
          class="navbar-toggler"
          type="button"
          data-toggle="collapse"
          data-target="#navbarSupportedContent"
          aria-controls="navbarSupportedContent"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <% if(user) { %>
          <ul class="navbar-nav ms-auto">
            <li class="nav-item">
              <a class="nav-link me-3 fs-5 mails text-dark" href="/mails">Mails</a>
            </li>
            <li class="nav-item">
              <a class="nav-link fs-5 sendMail text-dark" href="/sendMail">Send Mail</a>
            </li>
          </ul>
          <% }%>
          <ul class="navbar-nav ms-auto">
            <% if(!user) {%>
            <li class="nav-item me-3">
              <a
                href="/signup"
                class="text-dark px-lg-3 py-lg-1 px-2 py-1 mb-2 mb-lg-0 fw-bold rounded-pill d-lg-inline-block d-inline-block nav-link"
                >Sign up</a
              >
            </li>
            <li class="nav-item me-3">
              <a
                href="/login"
                class="text-white px-3 py-1 fw-bold rounded-pill d-lg-inline-block d-inline-block bg-primary nav-link"
                >Login</a
              >
            </li>
            <% } %> <% if(user) { %>
            <li class="nav-item me-3">
              <a
                href="/logout"
                class="text-dark fw-bold logout fs-5 border px-lg-3 py-lg-1 d-lg-inline-block d-inline-block nav-link"
                >Logout</a
              >
            </li>
            <% } %>
          </ul>
        </div>
      </nav>
    </header>
    <main></main>
  </body>
</html>
